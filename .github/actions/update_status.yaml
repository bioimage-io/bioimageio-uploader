on:
    workflow_call:
        inputs:
            url:
                required: true
                type: string
        secrets:
            S3_TOKEN:
                required: false
                default: "S3 Token not set 😢"

jobs:
    update-status:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2

            - name: Run a one-line script
              run: echo "URL is: $url"
